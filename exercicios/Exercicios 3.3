1) a
2) d
3) c - O args[0] esta vazio, retorna nullPointException
4) f - Vou usar o comando -cp (ClassPath) e indicar o caminho (package) da classe em questão
5) h -  
Durante a compilação, para adicionar o arquivo programa.jar ao
classpath, devemos usar -cp programa.jar e, para especificar o arquivo
adequado, usamos b/A.java. Aopção que apresenta essas duas
características é javac -cp programa.jar:. b/A.java.